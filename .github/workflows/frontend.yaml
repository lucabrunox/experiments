name: Frontend build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build
        shell: bash
        working-directory: frontend
        run: |
          docker build --rm . -t frontend